JQuant a Java Platform for simulation and backtesting.
-------------------------------------------------------------

Components
- jquant-core (core components and interface)
- jquant-bloomberg (Bloomberg Data Provider Feed) 

Use maven as build system.
Set up the M2_REPO variable to point towards your Maven Local Repository
